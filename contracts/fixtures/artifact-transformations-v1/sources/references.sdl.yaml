name: transformation-reference-case
nodes:
  web:
    type: vm
    resources:
      ram: 1 GiB
      cpu: 1
  peer:
    type: switch
content:
  payload:
    type: file
    target: web
    path: /opt/payload
relationships:
  loop:
    type: connects_to
    source: web
    target: web
