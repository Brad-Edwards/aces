name: unsupported-control
variables:
  cpu_count:
    type: integer
    allowed_values: [1, 2]
    required: true
nodes:
  target:
    type: vm
    resources:
      ram: 1 gib
      cpu: ${cpu_count}
