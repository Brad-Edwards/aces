Added `runtime.service_listeners` so SDL inventories can model observed bind
addresses, ports, listener scope, process/service ownership, readiness evidence,
and published-port correlations without overloading `Node.services`.
