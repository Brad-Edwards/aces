### Fixed

- Fixed internal defects in the participant-runtime formal spec
  (`specs/formal/participant-runtime/README.md`): `mapping_loss` now has the
  closed vocabulary invariant I16 requires (examples updated); the declared
  observation delivery point is defined in terms of `VisibleHistory`
  admission; rollback now has explicit participant-visible-history rules
  (append-only, disclosure-as-observation, evidence-only invalidation);
  marking authorization and visibility projection are stated to compose
  conjunctively and fail closed on conflict; `EventClassificationOK`'s
  "makes a claim" condition is now decidable; invariant I2 states the minimal
  valid trace for fully opaque participants; the capability-meet rule
  distinguishes missing declarations from declared `unsupported`; and
  history-consistency/perfect-recall reconstruction references must resolve
  to governed registry entries with executable implementations and fixtures,
  with the cross-trace conjunct restated as a test-time obligation.
