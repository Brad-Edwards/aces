### Added — SDL surface for SSH server configuration (`Node.runtime.ssh_servers`): typed forced-command, accept-env allowlist, scoped `Match` rules, and adjacent sshd directives (allow/deny users and groups, authentication methods, password / pubkey / TTY toggles, chroot directory, authorized-keys file). Implements ADR-031.
