`aces-sdl` is now published to PyPI (`pip install aces-sdl`), with releases cut automatically on promotion of `dev` to `main` via conventional-commit-driven, tag-derived versioning (ADR 0006 pattern). The release publishes the corpus-bundled wheel + sdist over OIDC trusted publishing and attaches a CycloneDX SBOM to each GitHub Release.
