Add `runtime.orchestration_authorities` container-spawn authority inventory (engine, scope, spawn templates, lifecycle policy, realized children, and privilege class) with an executable `require_profile_for_privilege_class` guard and scenario-scoped `control_interface_ref` resolution that requires a read-write docker socket for `host_root_equivalent` authorities, plus generated schemas (DSL-137).
