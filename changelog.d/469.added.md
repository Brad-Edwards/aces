Add structured `runtime.datastore_services` mapping and template manifests so search-index schemas can carry bounded field counts, dynamic policy, digests, and evidence refs instead of name-only lists.
