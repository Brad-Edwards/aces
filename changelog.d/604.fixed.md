Made libvirt backend teardown idempotent: a DELETE for a domain or network that is already absent now succeeds as torn down (connection, permission, and ownership failures still fail closed), and a partial CREATE that defines a domain before it fails to start is now rolled back so no orphaned domains, networks, or seed media are left behind.
