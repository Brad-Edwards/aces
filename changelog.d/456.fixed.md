Tightened typed runtime relationship validation so proxy-upstream service refs resolve to real upstream services and service-integration auth principals resolve within the engine application's authorization store.
