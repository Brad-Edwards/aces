### Fixed

- Consolidated runtime service-family registration so public exports, module namespacing aliases, and semantic runtime refs share one registry, including `ssh_servers`. Accepted `provenance` on runtime identity attributes as a checked synonym for the existing origin enum to keep TechVault capture facts lossless.
