Declared `packaging` as a runtime dependency of `aces-sdl`. It was imported by `aces_sdl.module_registry` on the CLI import path but only present transitively, so any `aces` command failed with `ModuleNotFoundError: No module named 'packaging'` in a clean wheel install.
